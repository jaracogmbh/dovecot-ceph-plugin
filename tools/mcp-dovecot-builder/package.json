{
  "name": "mcp-dovecot-builder",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server to build and test the dovecot-ceph-plugin via Docker Compose",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "lint": "node -e 'process.exit(0)'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  }
}
